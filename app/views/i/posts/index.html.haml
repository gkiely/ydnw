.page
  .content
    .index-bio
      .index-bio__left
        %h1= @user.name
        %h4= @user.bio
      .index-bio__right
        = image_tag(Avatar.new(@user.email).get)

    .posts
      - @posts.published.each do |post|
        .post
          = link_to(post.title)
